import 'package:flutter/material.dart';

class Pow extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow.shade700,
      width: 360,
      height: 360,
      child: Image.asset(
        'assets/images/roberto.png',
      ),
    );
  }
}
